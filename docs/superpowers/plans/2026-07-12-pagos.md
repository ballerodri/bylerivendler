# Pagos — Plan de implementación (Etapa 1a)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que la clienta pueda elegir pagar **la seña (30%) o el total** al reservar, y que el salón pueda **registrar cuánto cobró de verdad** en cada turno.

**Architecture:** Una regla pura y testeada define cuánto se pide pagar ahora (`amountDueNow`). `appointments.deposit_cents` pasa a significar "lo que debe pagar AHORA" (30% o 100%); una columna nueva `paid_cents` guarda lo efectivamente cobrado. **Ninguna plata se mueve entre turnos.**

**Tech Stack:** Next.js 16 (App Router, Server Actions), TypeScript strict, Supabase, Vitest, Zod.

**Spec:** `docs/superpowers/specs/2026-07-12-reserva-multiple-design.md`

## Global Constraints

- **NO hay pasarela de pago.** La app nunca cobra: la clienta transfiere y manda el comprobante por WhatsApp. Elegir "pagar el total" **sólo cambia el importe que se le pide transferir**.
- **La plata NO se mueve entre turnos.** Cada turno lleva su propio `total_cents`, su `deposit_cents` y su `paid_cents`. (Ver `docs/superpowers/specs/2026-07-12-pack-multi-sesion-design.md`: mover el precio entre turnos ya rompió facturación una vez.)
- **Semántica de las columnas:** `total_cents` = lo que vale el turno · `deposit_cents` = **lo que debe pagar AHORA** (seña 30% o el total) · `paid_cents` = **lo que el salón cobró**.
- `deposit_paid` (bool) **no se toca ni se borra** (lo usan flujos existentes).
- **Facturación y Estadísticas NO se tocan** (siguen leyendo `total_cents`).
- Money en centavos (int). La UI muestra pesos.
- **El canje con puntos no ofrece elegir**: si canjea, no hay nada que pagar.
- Verificación en cada tarea: `npx tsc --noEmit` = 0, `npx vitest run` verde, `npm run build` = 0.

---

### Task 1: Reglas puras de pago (TDD)

**Files:**
- Create: `src/lib/servicios/payments.ts`
- Test: `src/lib/servicios/payments.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `DEPOSIT_PCT = 0.3`
  - `type PayChoice = "deposit" | "full"`
  - `amountDueNow(totalCents: number, choice: PayChoice): number`
  - `paymentSummary(paidCents: number, totalCents: number): { paidCents, totalCents, pendingCents, isPaidInFull, isUnpaid }`
  - `validatePayment(paidCents: number, totalCents: number): { ok: true } | { ok: false; error: string }`

- [ ] **Step 1: Escribir el test que falla**

Crear `src/lib/servicios/payments.test.ts`:

```ts
import { describe, it, expect } from "vitest"
import { amountDueNow, paymentSummary, validatePayment, DEPOSIT_PCT } from "./payments"

describe("amountDueNow", () => {
  it("seña = 30% del total", () => {
    expect(amountDueNow(10_000_000, "deposit")).toBe(3_000_000)
  })

  it("total = el total completo", () => {
    expect(amountDueNow(10_000_000, "full")).toBe(10_000_000)
  })

  it("redondea al centavo (no deja fracciones)", () => {
    // 5.555.500 * 0.3 = 1.666.650 exacto
    expect(amountDueNow(5_555_500, "deposit")).toBe(1_666_650)
    // 3.333.333 * 0.3 = 999.999,9 -> redondea
    expect(amountDueNow(3_333_333, "deposit")).toBe(1_000_000)
  })

  it("total 0 (canje con puntos) -> no se paga nada, elija lo que elija", () => {
    expect(amountDueNow(0, "deposit")).toBe(0)
    expect(amountDueNow(0, "full")).toBe(0)
  })

  it("total negativo -> 0 (defensivo)", () => {
    expect(amountDueNow(-100, "deposit")).toBe(0)
  })

  it("DEPOSIT_PCT es 0.3", () => {
    expect(DEPOSIT_PCT).toBe(0.3)
  })
})

describe("paymentSummary", () => {
  it("sin cobrar", () => {
    expect(paymentSummary(0, 10_000_000)).toEqual({
      paidCents: 0, totalCents: 10_000_000, pendingCents: 10_000_000,
      isPaidInFull: false, isUnpaid: true,
    })
  })

  it("cobrada la seña (parcial)", () => {
    expect(paymentSummary(3_000_000, 10_000_000)).toEqual({
      paidCents: 3_000_000, totalCents: 10_000_000, pendingCents: 7_000_000,
      isPaidInFull: false, isUnpaid: false,
    })
  })

  it("cobrado todo", () => {
    expect(paymentSummary(10_000_000, 10_000_000)).toEqual({
      paidCents: 10_000_000, totalCents: 10_000_000, pendingCents: 0,
      isPaidInFull: true, isUnpaid: false,
    })
  })

  it("cobrado de más -> pendiente no baja de 0", () => {
    const r = paymentSummary(12_000_000, 10_000_000)
    expect(r.pendingCents).toBe(0)
    expect(r.isPaidInFull).toBe(true)
  })

  it("turno en 0 (canje/sesión de pack) no cuenta como pagado por defecto", () => {
    const r = paymentSummary(0, 0)
    expect(r.isPaidInFull).toBe(false)
    expect(r.pendingCents).toBe(0)
  })
})

describe("validatePayment", () => {
  it("monto válido", () => {
    expect(validatePayment(3_000_000, 10_000_000)).toEqual({ ok: true })
  })

  it("cobrar el total exacto es válido", () => {
    expect(validatePayment(10_000_000, 10_000_000)).toEqual({ ok: true })
  })

  it("cero es válido (desmarcar un cobro)", () => {
    expect(validatePayment(0, 10_000_000)).toEqual({ ok: true })
  })

  it("negativo -> error", () => {
    const r = validatePayment(-1, 10_000_000)
    expect(r.ok).toBe(false)
  })

  it("más que el total -> error", () => {
    const r = validatePayment(10_000_001, 10_000_000)
    expect(r).toEqual({ ok: false, error: "No podés registrar más de lo que vale el turno." })
  })

  it("no entero -> error", () => {
    const r = validatePayment(100.5, 10_000_000)
    expect(r.ok).toBe(false)
  })
})
```

- [ ] **Step 2: Correr el test y verificar que falla**

Run: `npx vitest run src/lib/servicios/payments.test.ts`
Expected: FAIL — "Failed to resolve import './payments'".

- [ ] **Step 3: Implementar**

Crear `src/lib/servicios/payments.ts`:

```ts
/**
 * Reglas de pago. Lógica PURA (sin servidor) para poder testearla y usar la
 * MISMA regla en la pantalla y en el servidor.
 *
 * OJO: en esta app NO hay pasarela de pago. La clienta transfiere por fuera y
 * manda el comprobante por WhatsApp. Estas funciones sólo definen CUÁNTO se le
 * pide pagar ahora, y cuánto se cobró.
 */

/** Porcentaje de la seña. */
export const DEPOSIT_PCT = 0.3

/** Qué eligió pagar ahora la clienta. */
export type PayChoice = "deposit" | "full"

/**
 * Cuánto tiene que pagar AHORA (va a `appointments.deposit_cents`).
 *  - "deposit" → la seña (30%); el resto lo abona en el local.
 *  - "full"    → el total; no debe nada al llegar.
 */
export function amountDueNow(totalCents: number, choice: PayChoice): number {
  if (totalCents <= 0) return 0
  return choice === "full" ? totalCents : Math.round(totalCents * DEPOSIT_PCT)
}

export type PaymentSummary = {
  paidCents: number
  totalCents: number
  /** Lo que falta cobrar (nunca negativo). */
  pendingCents: number
  isPaidInFull: boolean
  isUnpaid: boolean
}

/** Estado de cobro de un turno, para mostrarlo en el admin. */
export function paymentSummary(paidCents: number, totalCents: number): PaymentSummary {
  const paid = Math.max(0, paidCents)
  const total = Math.max(0, totalCents)
  return {
    paidCents: paid,
    totalCents: total,
    pendingCents: Math.max(0, total - paid),
    isPaidInFull: total > 0 && paid >= total,
    isUnpaid: paid === 0,
  }
}

/** Valida un cobro que el salón quiere registrar. */
export function validatePayment(
  paidCents: number,
  totalCents: number
): { ok: true } | { ok: false; error: string } {
  if (!Number.isInteger(paidCents))
    return { ok: false, error: "El monto tiene que ser un número entero." }
  if (paidCents < 0) return { ok: false, error: "El monto no puede ser negativo." }
  if (paidCents > totalCents)
    return { ok: false, error: "No podés registrar más de lo que vale el turno." }
  return { ok: true }
}
```

- [ ] **Step 4: Correr los tests y verificar que pasan**

Run: `npx vitest run src/lib/servicios/payments.test.ts`
Expected: PASS — **17 tests** (6 de `amountDueNow`, 5 de `paymentSummary`, 6 de `validatePayment`).

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 5: Commit**

```bash
git add src/lib/servicios/payments.ts src/lib/servicios/payments.test.ts
git commit -m "feat(pagos): reglas puras (cuánto se paga ahora, estado de cobro, validación)"
```

---

### Task 2: Migración — `appointments.paid_cents`

**Files:**
- Create: `supabase/migrations/20260712010000_paid_cents.sql`

**Interfaces:**
- Produces: la columna `appointments.paid_cents int not null default 0 check (paid_cents >= 0)`.

- [ ] **Step 1: Escribir la migración**

Crear `supabase/migrations/20260712010000_paid_cents.sql`:

```sql
-- Cuánto se cobró DE VERDAD de este turno (lo registra el salón).
-- Hasta ahora sólo existía deposit_paid (bool), que se escribía al crear el
-- turno y no había ninguna forma de modificarlo: era imposible anotar un cobro.
--
-- Semántica de las tres columnas (ninguna se mueve entre turnos):
--   total_cents   = lo que vale el turno
--   deposit_cents = lo que la clienta debe pagar AHORA (seña 30%, o el total si
--                   eligió pagar todo)
--   paid_cents    = lo que el salón efectivamente cobró
alter table public.appointments
  add column if not exists paid_cents int not null default 0
    check (paid_cents >= 0);
```

- [ ] **Step 2: Verificar que no rompe nada**

Run: `npx tsc --noEmit && npm run build`
Expected: ambos exit 0 (la migración todavía no se aplica; sólo se comprueba que el repo sigue sano).

- [ ] **Step 3: Commit**

> **La aplica la controladora**, pusheándola **sola** primero (el CI la corre) y verificándola contra la base ANTES de desplegar el código que la consulta. No la mezcles con código.

```bash
git add supabase/migrations/20260712010000_paid_cents.sql
git commit -m "chore(db): appointments.paid_cents (columna antes que el código)"
```

---

### Task 3: El servidor acepta la elección de pago

**Files:**
- Modify: `src/lib/servicios/pack-sessions.ts` (`packSessionPrices` acepta la elección)
- Modify: `src/lib/servicios/pack-sessions.test.ts` (un test nuevo)
- Modify: `src/app/reserva/actions.ts` (schema + cálculo de la seña + inserts)

**Interfaces:**
- Consumes de Task 1: `amountDueNow`, `type PayChoice`.
- Produces: `createBooking` acepta `payChoice?: "deposit" | "full"` (default `"deposit"`); `packSessionPrices(totalPriceCents, count, payChoice?)`.

- [ ] **Step 1: `packSessionPrices` acepta la elección**

En `src/lib/servicios/pack-sessions.ts`, importar arriba:

```ts
import { amountDueNow, type PayChoice } from "./payments"
```

Y reemplazar `packSessionPrices` por:

```ts
/**
 * Reparte el precio del pack entre sus turnos: la 1ª sesión lleva el precio
 * completo y las demás van en 0 (ya vienen pagadas por el pack). Así el pack se
 * cuenta UNA sola vez en facturación/estadísticas.
 *
 * `payChoice` define cuánto se le pide pagar AHORA por la 1ª sesión: la seña
 * (30%) o el total del pack.
 *
 * ⚠️ LLAMAR UNA SOLA VEZ POR COMPRA, al crear el pack. Una acción que agende las
 * sesiones RESTANTES de un pack ya existente NO debe llamarla: el índice 0
 * pondría el precio completo del pack en una sesión posterior y lo cobraría dos
 * veces.
 */
export function packSessionPrices(
  totalPriceCents: number,
  count: number,
  payChoice: PayChoice = "deposit"
): PackSessionPrice[] {
  return Array.from({ length: count }, (_, i) =>
    i === 0
      ? {
          totalCents: totalPriceCents,
          depositCents: amountDueNow(totalPriceCents, payChoice),
          depositPaid: false,
        }
      : { totalCents: 0, depositCents: 0, depositPaid: true }
  )
}
```

- [ ] **Step 2: Test del caso nuevo**

En `src/lib/servicios/pack-sessions.test.ts`, actualizar el import y agregar tests dentro del `describe("packSessionPrices", ...)` existente:

```ts
  it("si eligió pagar el TOTAL, la 1ª sesión pide el precio completo del pack", () => {
    const r = packSessionPrices(17_000_000, 3, "full")
    expect(r[0]).toEqual({ totalCents: 17_000_000, depositCents: 17_000_000, depositPaid: false })
    expect(r[1]).toEqual({ totalCents: 0, depositCents: 0, depositPaid: true })
  })

  it("por defecto sigue siendo la seña del 30% (no rompe lo existente)", () => {
    expect(packSessionPrices(17_000_000, 1)[0].depositCents).toBe(5_100_000)
  })
```

- [ ] **Step 3: `createBooking` acepta `payChoice`**

En `src/app/reserva/actions.ts`:

1. Importar arriba (junto a los otros de `@/lib/servicios`):

```ts
import { amountDueNow, type PayChoice } from "@/lib/servicios/payments"
```

2. En el schema `BookingInput`, agregar junto a `packSlots`:

```ts
  payChoice: z.enum(["deposit", "full"]).optional(),
```

3. Reemplazar el cálculo de la seña (la línea `const depositCents = Math.round(totalCents * 0.3)`):

```ts
  // Lo que se le pide pagar AHORA: la seña (30%) o el total, según eligió.
  const payChoice: PayChoice = input.payChoice ?? "deposit"
  const depositCents = amountDueNow(totalCents, payChoice)
```

4. En la rama del pack, pasar la elección a `packSessionPrices` (la línea `const prices = packSessionPrices(pack.total_price_cents, slotDates.length)`):

```ts
    const prices = packSessionPrices(pack.total_price_cents, slotDates.length, payChoice)
```

> `payChoice` queda declarado en el paso 3 (junto a `depositCents`, ~línea 150), que está **antes** de
> la rama `if (input.packId)` (~línea 188). Por lo tanto está en alcance dentro de la rama del pack.

5. En el insert del turno normal (no-pack), agregar `paid_cents` junto a `deposit_cents`:

```ts
      deposit_cents: redeem ? 0 : depositCents,
      deposit_paid: redeem,
      paid_cents: 0,
```

6. En el insert de cada turno del pack, agregar también:

```ts
          deposit_paid: price.depositPaid,
          paid_cents: 0,
```

- [ ] **Step 4: Verificar**

Run: `npx tsc --noEmit && npx vitest run && npm run build`
Expected: los tres en 0. Vitest: los 41 previos + 17 de Task 1 + 2 nuevos = **60**.

- [ ] **Step 5: Commit**

```bash
git add src/lib/servicios/pack-sessions.ts src/lib/servicios/pack-sessions.test.ts src/app/reserva/actions.ts
git commit -m "feat(pagos): createBooking acepta pagar la seña o el total"
```

---

### Task 4: La clienta elige cuánto pagar

**Files:**
- Modify: `src/app/reserva/data.ts` (`BookingState.payChoice`)
- Modify: `src/app/reserva/screens.tsx` (`Screen5Confirm`)

**Interfaces:**
- Consumes de Task 1: `amountDueNow`, `type PayChoice`.
- Consumes de Task 3: `createBooking` acepta `payChoice`.
- Produces: `state.payChoice` poblado y enviado al servidor.

- [ ] **Step 1: El estado**

En `src/app/reserva/data.ts`, en `BookingState` agregar después de `redeemWithPoints`:

```ts
  // Cuánto eligió pagar ahora: la seña (30%) o el total. Default: "deposit".
  payChoice?: "deposit" | "full"
```

- [ ] **Step 2: La elección en la pantalla de confirmación**

En `src/app/reserva/screens.tsx`:

1. Importar arriba:

```tsx
import { amountDueNow, type PayChoice } from "@/lib/servicios/payments"
```

2. En `Screen5Confirm`, reemplazar el cálculo de la seña (las líneas `const deposit = redeeming ? 0 : Math.round(total * 0.3)` y `const remaining = redeeming ? 0 : total - deposit`):

```tsx
  const payChoice: PayChoice = state.payChoice ?? "deposit"
  // Se calcula en CENTAVOS, igual que el servidor, para que no haya diferencias
  // de redondeo entre lo que ve la clienta y lo que se guarda.
  const depositCents = redeeming ? 0 : amountDueNow(Math.round(total * 100), payChoice)
  const deposit = depositCents / 100
  const remaining = redeeming ? 0 : total - deposit

  const setPayChoice = (c: PayChoice) => setState({ ...state, payChoice: c })
```

3. Reemplazar el bloque del desglose que muestra la seña (el que hoy dice `Resto a abonar en el local` y `Seña (30%) hoy`) por:

```tsx
          <>
            {remaining > 0 && (
              <div className="breakdown__row">
                <span>Resto a abonar en el local</span>
                <span>{fmtPrice(remaining)}</span>
              </div>
            )}
            <div className="breakdown__row breakdown__row--total">
              <span>{payChoice === "full" ? "Total a pagar hoy" : "Seña (30%) hoy"}</span>
              <span>{fmtPrice(deposit)}</span>
            </div>
          </>
```

4. Justo **antes** del recuadro de "Seña por transferencia" (el `{!redeeming && (` que abre el bloque `mp-badge`), insertar la elección:

```tsx
      {!redeeming && total > 0 && (
        <div style={{ margin: "16px 0", display: "flex", flexDirection: "column", gap: 8 }}>
          <strong style={{ fontFamily: "var(--serif)", fontSize: 15 }}>¿Cuánto vas a pagar ahora?</strong>
          {([
            { v: "deposit" as const, label: "La seña (30%)", note: "El resto lo abonás en el local" },
            { v: "full" as const, label: "El total", note: "No debés nada al llegar" },
          ]).map((o) => (
            <label
              key={o.v}
              style={{
                display: "flex", alignItems: "center", gap: 10, cursor: "pointer",
                padding: "12px 14px", borderRadius: 12, fontSize: 13,
                border: `1px solid ${payChoice === o.v ? "var(--nude)" : "var(--line)"}`,
                background: payChoice === o.v ? "var(--rose-wash)" : "transparent",
              }}
            >
              <input
                type="radio"
                name="payChoice"
                checked={payChoice === o.v}
                onChange={() => setPayChoice(o.v)}
                style={{ width: 16, height: 16 }}
              />
              <span style={{ flex: 1 }}>
                <strong>{o.label}</strong>
                <br />
                <span style={{ color: "var(--ink-soft)", fontSize: 12 }}>{o.note}</span>
              </span>
              <strong>{fmtPrice(amountDueNow(Math.round(total * 100), o.v) / 100)}</strong>
            </label>
          ))}
        </div>
      )}
```

5. En el recuadro "Seña por transferencia", cambiar el título para que no mienta cuando eligió pagar todo. Reemplazar el texto `Seña por transferencia` por:

```tsx
            {payChoice === "full" ? "Pago por transferencia" : "Seña por transferencia"}
```

6. En el payload de `createBooking`, agregar junto a `redeemWithPoints`:

```tsx
      payChoice,
```

- [ ] **Step 3: Verificar**

Run: `npx tsc --noEmit && npx vitest run && npm run build`
Expected: los tres en 0.

- [ ] **Step 4: Verificación manual (obligatoria)**

```bash
npm run dev
```

En `/reserva`: elegir un servicio, llegar a la confirmación y comprobar:
1. Aparece "¿Cuánto vas a pagar ahora?" con las dos opciones y **el importe de cada una**.
2. Al elegir **El total**, el desglose muestra "Total a pagar hoy" y **desaparece** "Resto a abonar en el local".
3. Al elegir **La seña**, vuelve a mostrar el 30% y el resto.
4. Con **canje de puntos** activado, la elección **no aparece** (no hay nada que pagar).
5. Reservar con "El total" → en Supabase, el turno tiene `deposit_cents == total_cents`.

- [ ] **Step 5: Commit**

```bash
git add src/app/reserva/data.ts src/app/reserva/screens.tsx
git commit -m "feat(pagos): la clienta elige pagar la seña o el total al reservar"
```

---

### Task 5: El salón registra cuánto cobró

**Files:**
- Modify: `src/app/admin/actions.ts` (acción `registrarPago`)
- Modify: `src/app/admin/_components/status-actions.tsx` (ítem "Registrar pago" + formulario)
- Modify: `src/app/admin/turnos/page.tsx` (traer `paid_cents`, mostrar el estado, pasar la prop)

**Interfaces:**
- Consumes de Task 1: `validatePayment`, `paymentSummary`.
- Produces:
  - `registrarPago(appointmentId: string, paidCents: number): Promise<{ ok: boolean; error?: string }>`
  - `StatusActions` gana la prop `paidCents: number` (obligatoria; ya tiene `totalCents`).

- [ ] **Step 1: La acción del servidor**

En `src/app/admin/actions.ts`, importar arriba:

```ts
import { validatePayment } from "@/lib/servicios/payments"
```

Y agregar la acción (junto a las demás de turnos):

```ts
/**
 * Registra cuánto se cobró de un turno. NO toca `total_cents` (la plata no se
 * mueve): sólo anota lo efectivamente recibido.
 */
export async function registrarPago(
  appointmentId: string,
  paidCents: number
): Promise<{ ok: boolean; error?: string }> {
  await requireStaff()
  const admin = adminClient()

  const { data: appt } = await admin
    .from("appointments")
    .select("total_cents")
    .eq("id", appointmentId)
    .maybeSingle()
  if (!appt) return { ok: false, error: "Turno no encontrado." }

  const v = validatePayment(paidCents, appt.total_cents as number)
  if (!v.ok) return { ok: false, error: v.error }

  const { error } = await admin
    .from("appointments")
    .update({ paid_cents: paidCents })
    .eq("id", appointmentId)
  if (error) return { ok: false, error: error.message }

  revalidatePath("/admin")
  revalidatePath("/admin/turnos")
  revalidatePath("/admin/clientas")
  return { ok: true }
}
```

- [ ] **Step 2: El formulario en `StatusActions`**

En `src/app/admin/_components/status-actions.tsx`:

1. Importar la acción junto a las demás:

```tsx
import { updateAppointmentStatus, deleteAppointment, registrarPago } from "../actions"
```

2. Agregar `paidCents` a las props (ya existe `totalCents`):

```tsx
  paidCents,
```

y en el tipo:

```tsx
  paidCents: number
```

3. Agregar el estado, junto a los otros `useState`:

```tsx
  const [payingOpen, setPayingOpen] = useState(false)
  const [payInput, setPayInput] = useState("")
```

4. Agregar el handler, junto a `handleDelete`:

```tsx
  const savePago = () => {
    setError(null)
    const pesos = parseInt(payInput, 10)
    if (isNaN(pesos) || pesos < 0) { setError("Monto inválido"); return }
    startTransition(async () => {
      const r = await registrarPago(appointmentId, pesos * 100)
      if (r.ok) setPayingOpen(false)
      else setError(r.error ?? "Error")
    })
  }
```

5. Agregar el formulario **antes** del `if (confirmDelete)` existente:

```tsx
  if (payingOpen) {
    return (
      <>
        <span style={{ fontSize: 12, color: "var(--ink-mute)", whiteSpace: "nowrap" }}>Cobrado $</span>
        <input
          type="number"
          min="0"
          value={payInput}
          onChange={(e) => setPayInput(e.target.value)}
          className="adm-input"
          style={{ width: 100, fontSize: 13, textAlign: "right" }}
          autoFocus
        />
        <button className="adm-btn adm-btn--primary" disabled={pending} onClick={savePago}>Guardar</button>
        <button className="adm-btn" onClick={() => setPayingOpen(false)}>Cancelar</button>
        {error && <span style={{ fontSize: 10, color: "#8c463c" }}>{error}</span>}
      </>
    )
  }
```

6. Agregar el ítem al menú `⋯`, **arriba** del separador y de "Eliminar":

```tsx
        {totalCents > 0 && (
          <button
            type="button"
            role="menuitem"
            disabled={pending}
            onClick={() => {
              // Se pre-carga con lo que falta cobrar.
              setPayInput(String(Math.round(Math.max(0, totalCents - paidCents) / 100)))
              setPayingOpen(true)
            }}
          >
            Registrar pago
          </button>
        )}
```

- [ ] **Step 3: Traer `paid_cents` y mostrar el estado en la lista de turnos**

En `src/app/admin/turnos/page.tsx`:

1. Importar arriba:

```tsx
import { paymentSummary } from "@/lib/servicios/payments"
```

2. Agregar al tipo `ApptRow` (junto a `total_cents`):

```ts
  paid_cents: number
```

3. Agregarlo al `select` de `appointments` (junto a `total_cents, pack_purchase_id,`):

```
      id, starts_at, status, duration_min, total_cents, paid_cents, pack_purchase_id,
```

4. En la fila, **debajo** del precio, mostrar el estado de cobro. En el `<div className="adm-sub">` (y en la rama `isMulti`, en el `<div>` que muestra `{a.duration_min} min · <strong>{fmtPrice(...)}</strong>`), agregar a continuación:

```tsx
                      {a.total_cents > 0 && (() => {
                        const p = paymentSummary(a.paid_cents, a.total_cents)
                        return (
                          <span
                            style={{
                              marginLeft: 8,
                              fontSize: 12,
                              color: p.isPaidInFull ? "#4d6b3e" : p.isUnpaid ? "#8c463c" : "var(--ink-mute)",
                            }}
                          >
                            {p.isPaidInFull
                              ? "· Pagado ✓"
                              : `· Pagado ${fmtPrice(p.paidCents / 100)} de ${fmtPrice(p.totalCents / 100)}`}
                          </span>
                        )
                      })()}
```

5. Pasar la prop nueva a `StatusActions`:

```tsx
                  <StatusActions
                    appointmentId={a.id}
                    currentStatus={a.status}
                    matchingPacks={packsForAppt(a)}
                    packLinked={!!a.pack_purchase_id}
                    totalCents={a.total_cents}
                    paidCents={a.paid_cents}
                  />
```

6. **`src/app/admin/page.tsx` (dashboard "Hoy") también renderiza `StatusActions`.** Como `paidCents` es obligatoria, hay que agregarla ahí también: sumar `paid_cents` a su tipo `ApptRow` y a su `select`, y pasar `paidCents={a.paid_cents}`.

- [ ] **Step 4: Verificar**

Run: `npx tsc --noEmit && npx vitest run && npm run build`
Expected: los tres en 0.

- [ ] **Step 5: Verificación manual (obligatoria)**

En `/admin/turnos`:
1. Un turno sin cobrar muestra **"Pagado $0 de $X"** en rojo.
2. Menú **⋯ → Registrar pago** → el campo viene pre-cargado con lo que falta → guardar.
3. La fila pasa a **"Pagado $Y de $X"**; si se cobró todo, **"Pagado ✓"** en verde.
4. Intentar registrar **más** que el total → error *"No podés registrar más de lo que vale el turno."*
5. Un turno en **$0** (sesión de pack o canje) **no** ofrece "Registrar pago".

- [ ] **Step 6: Commit**

```bash
git add src/app/admin/actions.ts src/app/admin/_components/status-actions.tsx src/app/admin/turnos/page.tsx src/app/admin/page.tsx
git commit -m "feat(pagos): registrar cuánto se cobró de cada turno desde el admin"
```

---

### Task 6: Verificación final y deploy

**Files:** ninguno (sólo verificación).

- [ ] **Step 1: Suite completa**

```bash
npx tsc --noEmit && npx vitest run && npm run build
```
Expected: los tres en 0. Vitest: 41 previos + 17 (Task 1) + 2 (Task 3) = **60**.

- [ ] **Step 2: Recorrido end-to-end en dev**

1. Reservar eligiendo **"El total"** → el turno queda con `deposit_cents == total_cents`.
2. Reservar eligiendo **"La seña"** → `deposit_cents == 30%`.
3. Reservar **canjeando puntos** → no aparece la elección; `total_cents = 0`, `deposit_cents = 0`.
4. En el admin, **Registrar pago** por el total → la fila muestra **"Pagado ✓"**.
5. **Facturar** un turno cobrado → sigue emitiendo por `total_cents` (la facturación **no cambió**).
6. **Estadísticas** → los números **no cambiaron** (siguen leyendo `total_cents`).

- [ ] **Step 3: Deploy (ordenado — lo hace la controladora)**

La migración va **primero y sola** (el CI la aplica), se **verifica contra la base**, y recién ahí se pushea el código. Nunca juntas.

---

## Notas de riesgo

- **`deposit_cents` cambia de significado** (antes: siempre el 30%; ahora: lo que debe pagar ahora, que puede ser el total). Ningún consumidor lo usa para calcular ingresos (Estadísticas y Facturación leen `total_cents`) — **verificarlo** antes de mergear.
- **`StatusActions` se renderiza en dos lugares** (`admin/turnos/page.tsx` y `admin/page.tsx`). `paidCents` es obligatoria: si falta en uno, es error de compilación (deseado).
- **No tocar `deposit_paid`**: sigue existiendo y hay flujos que lo escriben. `paid_cents` es la fuente de verdad nueva.
